<div class="contents row">

  <p><%= @nickname %>さんの投稿一覧</p>
  <h1><%= link_to "投稿一覧へ戻る",tweets_path,method: :get%></h1>
  
  <% @tweets.each do |tweet| %>
    <div class="content_post" style="background-image: url(<%= tweet.image %>);">
      <p><%= tweet.text %></p>
      <%= image_tag tweet.image.url %>
       </div>
       
  <% end %>
  
</div>